"""  
        Inheritance:
          #  Inheritance is the process where one class inherits(receives) the properties(attributes) and methods
            of another class.

        # The class which inherits(receives) the properties(attributes) and methods is known as chlid class(or sub class or derived class).


        # The class whose properties and methods are inherited is known as parent class(or super class or base class).


        syntax
        class Parent:
            #properties
            #methods

        class Child(Parent):
            #properties
            #methods

        #================== Types of Inheritance =========================
        1. Single Inheritance
        2. Multi-level inheritance
        3. Multiple inheritance
        4. Hierarchical inheritance.
        5. Hybrid inheritance.

        
"""